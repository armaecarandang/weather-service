# Devops Configuration

This directory is reserved for environment-specific deployment and configuration files.

`devops/local/` is intentionally ignored by Git because it can contain machine-local settings and API keys.

Keep committed files in this directory free of secrets.
